{
  "scripts": {
    "dev": "bash scripts/dev_preview.sh",
    "frontend:install": "npm --prefix frontend install --no-audit --no-fund",
    "build": "npm run frontend:install && npm --prefix frontend run build",
    "build:dev": "npm run frontend:install && npm --prefix frontend run build",
    "typecheck": "npm run frontend:install && npm --prefix frontend run typecheck",
    "backend": "python3 -m uvicorn backend.app_factory:create_app --factory --host 127.0.0.1 --port 8000"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0",
    "@tauri-apps/cli": "^2.0.0-rc.18"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.9"
  }
}
